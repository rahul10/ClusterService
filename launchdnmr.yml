---
 - hosts: combo
   tasks:
    - name: starting  datanodes  
      shell: '/hadoop2/sbin/hadoop-daemon.sh  start datanode'
    - name: starting  nodemagers 
      shell: '/hadoop2/sbin/yarn-daemon.sh  start nodemanager'
    
